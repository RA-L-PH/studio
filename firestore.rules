
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /queues/{queueId} {
      allow read, write: if true; // Public for MVP clinic flow
    }
    match /metrics/clinic_stats {
      allow read, write: if true;
    }
  }
}
